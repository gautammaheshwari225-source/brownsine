<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
    <title>GM Store | Luxury Aesthetic — Full Control</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: 'Inter', sans-serif;
            background: #0a0a0a;
            color: #e5e5e5;
            scroll-behavior: smooth;
        }
        ::-webkit-scrollbar { width: 6px; }
        ::-webkit-scrollbar-track { background: #1a1a1a; }
        ::-webkit-scrollbar-thumb { background: #d4af37; border-radius: 8px; }
        :root {
            --gold: #d4af37;
            --gold-light: #f3e5ab;
            --dark-bg: #000000;
            --glass-border: rgba(212, 175, 55, 0.25);
            --shadow-sm: 0 20px 35px -12px rgba(0,0,0,0.5);
            --shadow-gold: 0 10px 25px -5px rgba(212,175,55,0.2);
        }
        .glass {
            background: rgba(10, 10, 10, 0.65);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(212, 175, 55, 0.2);
            border-radius: 2rem;
        }
        .container { max-width: 1400px; margin: 0 auto; padding: 0 2rem; }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1.2rem 2rem;
            background: rgba(0,0,0,0.85);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(212,175,55,0.3);
            position: sticky;
            top: 0;
            z-index: 100;
            flex-wrap: wrap;
        }
        .logo-area { display: flex; align-items: center; gap: 0.75rem; }
        .gm-monogram {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, #fff 30%, var(--gold) 80%);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            letter-spacing: -1px;
            border-right: 2px solid var(--gold);
            padding-right: 1rem;
        }
        .gm-logo-symbol {
            width: 48px;
            height: 48px;
            background: linear-gradient(145deg, #1a1a1a, #000);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            border: 1px solid var(--gold);
            box-shadow: 0 0 12px rgba(212,175,55,0.4);
        }
        .gm-logo-symbol span { font-size: 1.9rem; font-weight: 800; background: linear-gradient(135deg, #e9c468, #b48c2c); -webkit-background-clip: text; background-clip: text; color: transparent; }
        .nav-links { display: flex; gap: 1.5rem; align-items: center; flex-wrap: wrap; }
        .nav-links a { color: #ddd; text-decoration: none; font-weight: 500; transition: 0.2s; cursor: pointer; }
        .nav-links a:hover { color: var(--gold); }
        .cart-icon, .wishlist-icon { font-size: 1.3rem; cursor: pointer; position: relative; }
        .cart-count { position: absolute; top: -8px; right: -12px; background: var(--gold); color: black; font-size: 0.7rem; font-weight: bold; border-radius: 50%; width: 18px; height: 18px; display: flex; align-items: center; justify-content: center; }
        .search-bar { background: rgba(30,30,30,0.7); border: 1px solid rgba(212,175,55,0.4); border-radius: 2rem; padding: 0.4rem 1rem; display: flex; align-items: center; gap: 0.5rem; }
        .search-bar input { background: transparent; border: none; outline: none; color: white; font-size: 0.9rem; width: 160px; }
        .search-bar i { color: var(--gold); }
        .hero { min-height: 75vh; display: flex; align-items: center; justify-content: center; text-align: center; background: radial-gradient(circle at 20% 30%, #0f0f0f, #000000); border-bottom: 1px solid rgba(212,175,55,0.2); }
        .hero-content h1 { font-size: 5rem; letter-spacing: -2px; }
        .gm-logo-large { font-size: 7rem; font-weight: 800; background: linear-gradient(135deg, #ffffff, #d4af37, #b48c2c); -webkit-background-clip: text; background-clip: text; color: transparent; margin-bottom: 1rem; }
        .tagline { font-size: 1.5rem; letter-spacing: 2px; color: var(--gold-light); font-weight: 300; }
        section { padding: 4rem 0; }
        .section-title { font-size: 2rem; font-weight: 600; margin-bottom: 2rem; position: relative; display: inline-block; }
        .section-title:after { content: ''; position: absolute; bottom: -10px; left: 0; width: 70px; height: 3px; background: var(--gold); }
        .products-grid, .categories-grid, .bestsellers-grid, .testimonials-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 2rem; }
        .product-card { background: rgba(18,18,18,0.9); backdrop-filter: blur(4px); border-radius: 1.5rem; overflow: hidden; transition: all 0.35s ease; border: 1px solid rgba(212,175,55,0.2); box-shadow: var(--shadow-sm); cursor: pointer; }
        .product-card:hover { transform: translateY(-8px); border-color: var(--gold); box-shadow: var(--shadow-gold); }
        .product-img { height: 240px; background: #111; display: flex; align-items: center; justify-content: center; font-size: 3.5rem; color: var(--gold); }
        .product-info { padding: 1.2rem; }
        .product-title { font-weight: 600; font-size: 1rem; }
        .product-price { color: var(--gold); font-weight: 700; margin: 0.5rem 0; }
        .card-actions { display: flex; justify-content: space-between; margin-top: 1rem; }
        .btn-add, .btn-wish { background: rgba(212,175,55,0.1); border: 1px solid rgba(212,175,55,0.4); padding: 0.4rem 1rem; border-radius: 2rem; color: white; cursor: pointer; transition: 0.2s; font-size: 0.8rem; }
        .btn-add:hover, .btn-wish:hover { background: var(--gold); color: black; border-color: var(--gold); }
        .category-card { background: linear-gradient(145deg, #141414, #0a0a0a); border-radius: 1.5rem; text-align: center; padding: 2rem; transition: 0.3s; cursor: pointer; border: 1px solid rgba(212,175,55,0.3); }
        .category-card i { font-size: 3rem; color: var(--gold); margin-bottom: 1rem; }
        .testimonial-card { background: rgba(20,20,20,0.7); backdrop-filter: blur(4px); border-radius: 1.5rem; padding: 1.5rem; border-left: 4px solid var(--gold); }
        .offer-banner { background: linear-gradient(115deg, #000000, #1e1a0c); border-radius: 2rem; padding: 2.5rem; text-align: center; border: 1px solid var(--gold); margin: 1rem 0 2rem; }
        .offer-banner h3 { font-size: 1.8rem; color: var(--gold); }
        footer { background: #020202; padding: 2rem; text-align: center; border-top: 1px solid rgba(212,175,55,0.3); }
        .social-links i { font-size: 1.5rem; margin: 0 0.8rem; color: #aaa; transition: 0.2s; cursor: pointer; }
        .social-links i:hover { color: var(--gold); }
        .cart-sidebar, .checkout-page, .admin-panel {
            position: fixed;
            top: 0;
            right: -480px;
            width: 440px;
            height: 100%;
            background: #0a0a0a;
            backdrop-filter: blur(20px);
            border-left: 1px solid var(--gold);
            box-shadow: -5px 0 30px rgba(0,0,0,0.8);
            transition: right 0.4s cubic-bezier(0.2,0.9,0.4,1.1);
            z-index: 1000;
            padding: 2rem;
            overflow-y: auto;
        }
        .cart-sidebar.open, .checkout-page.open, .admin-panel.open { right: 0; }
        .close-panel { position: absolute; top: 1rem; right: 1.5rem; font-size: 1.8rem; cursor: pointer; color: var(--gold); }
        .cart-item { display: flex; justify-content: space-between; margin: 1rem 0; border-bottom: 1px solid #2a2a2a; padding-bottom: 0.8rem; align-items: center; }
        .btn-gold { background: var(--gold); border: none; padding: 0.8rem 1.5rem; font-weight: bold; border-radius: 2rem; cursor: pointer; color: black; }
        .btn-checkout { background: var(--gold); width: 100%; padding: 0.8rem; border-radius: 2rem; font-weight: bold; margin-top: 1rem; border: none; cursor: pointer; }
        .admin-btn { background: rgba(212,175,55,0.2); border: 1px solid var(--gold); padding: 0.3rem 0.8rem; border-radius: 2rem; font-size: 0.8rem; cursor: pointer; color: var(--gold); }
        .control-input { width: 100%; margin: 0.5rem 0; padding: 0.6rem; background: #1e1e1e; border: 1px solid #333; color: white; border-radius: 1rem; }
        @media (max-width: 768px) {
            .navbar { flex-direction: column; gap: 1rem; }
            .cart-sidebar { width: 100%; right: -100%; }
        }
        .toast-msg { position: fixed; bottom: 20px; left: 20px; background: #1a1a1a; border-left: 4px solid var(--gold); padding: 12px 20px; border-radius: 12px; z-index: 2000; color: white; font-weight: 500; backdrop-filter: blur(8px); }
    </style>
</head>
<body>

<div class="navbar">
    <div class="logo-area">
        <div class="gm-logo-symbol"><span>GM</span></div>
        <div class="gm-monogram">GAUTAM MAHESHWARI</div>
    </div>
    <div class="nav-links">
        <a data-nav="home">Home</a>
        <a data-nav="featured">Shop</a>
        <a data-nav="about">About</a>
        <a data-nav="contact">Contact</a>
        <div class="search-bar">
            <i class="fas fa-search"></i>
            <input type="text" id="searchInput" placeholder="Search...">
        </div>
        <div class="wishlist-icon" id="globalWishlistBtn"><i class="far fa-heart"></i></div>
        <div class="cart-icon" id="cartIcon"><i class="fas fa-shopping-bag"></i><span class="cart-count" id="cartCount">0</span></div>
        <div class="admin-btn" id="adminControlBtn"><i class="fas fa-sliders-h"></i> Control</div>
    </div>
</div>

<section class="hero" id="home"><div class="hero-content"><div class="gm-logo-large">GM</div><div class="tagline">Style with Aesthetic Look</div><p style="margin-top:1rem;">Luxury essentials — redefine elegance</p></div></section>

<div class="container">
    <section id="featured"><h2 class="section-title">Iconic Collection</h2><div class="products-grid" id="featuredGrid"></div></section>
    <section id="categories"><h2 class="section-title">Shop by Category</h2><div class="categories-grid" id="categoriesGrid"></div></section>
    <section id="bestsellers"><h2 class="section-title">Best Sellers</h2><div class="bestsellers-grid" id="bestsellersGrid"></div></section>
    <div class="offer-banner" id="offerBanner"><h3>✦ LIMITED OFFER ✦</h3><p>Get 15% off on first order | Code: GMGOLD15</p><button class="btn-gold" id="editOfferBtn" style="margin-top:1rem; background:#222; color:var(--gold);">✎ Edit Offer</button></div>
    <section id="about"><h2 class="section-title">About Gautam Maheshwari Store</h2><div style="max-width:800px; background:rgba(255,255,255,0.03); padding:2rem; border-radius:2rem;" id="aboutTextBlock"><p>GM Store represents the pinnacle of modern luxury — curated by Gautam Maheshwari. Each piece blends timeless elegance with contemporary design.</p></div><button id="editAboutBtn" class="admin-btn" style="margin-top:1rem;">✎ Edit About</button></section>
    <section><h2 class="section-title">Loved by Connoisseurs</h2><div class="testimonials-grid" id="testimonialsGrid"></div><button id="addTestimonialBtn" class="admin-btn" style="margin-top:1rem;">+ Add Testimonial</button></section>
</div>

<footer><div class="social-links"><i class="fab fa-instagram"></i><i class="fab fa-twitter"></i><i class="fab fa-facebook-f"></i><i class="fab fa-youtube"></i></div><p>© 2025 GM Store — Gautam Maheshwari. Full Control Edition.</p></footer>

<!-- Cart Sidebar -->
<div id="cartSidebar" class="cart-sidebar"><div class="close-panel" id="closeCartBtn">&times;</div><h2 style="color:var(--gold);">Your Bag</h2><div id="cartItemsList"></div><div id="cartTotal" style="margin-top:1rem;"></div><button id="checkoutBtnSidebar" class="btn-checkout">Proceed to Checkout</button></div>
<div id="checkoutPage" class="checkout-page"><div class="close-panel" id="closeCheckoutBtn">&times;</div><h2 style="color:var(--gold);">Secure Checkout</h2><input type="text" placeholder="Card Number •••• 4242" id="cardInput"><input type="text" placeholder="Expiry MM/YY"><input type="text" placeholder="CVC"><button id="payNowBtn" class="btn-gold" style="margin-top:1rem;">Pay Now — Secure</button><p style="margin-top:1rem;">🔒 GM Luxury Payment</p></div>

<!-- ADMIN CONTROL PANEL -->
<div id="adminPanel" class="admin-panel"><div class="close-panel" id="closeAdminBtn">&times;</div><h2 style="color:var(--gold);"><i class="fas fa-crown"></i> Store Control</h2>
    <label>Add New Product</label>
    <input type="text" id="newProdName" placeholder="Product Name" class="control-input">
    <input type="number" id="newProdPrice" placeholder="Price $" class="control-input">
    <select id="newProdCat" class="control-input"><option>Fashion</option><option>Accessories</option><option>Lifestyle</option><option>Trending</option></select>
    <button id="addProductBtn" class="btn-gold" style="width:100%; margin-top:0.5rem;">+ Add Product</button>
    <hr style="margin:1rem 0; border-color:var(--gold);">
    <label>Edit / Delete Products</label>
    <div id="adminProductList"></div>
    <hr>
    <label>Customize Theme Color</label>
    <input type="color" id="themeColorPicker" value="#d4af37">
    <button id="applyColorBtn" class="admin-btn">Apply Gold Accent</button>
    <hr>
    <label>Reset Demo Data</label>
    <button id="resetDataBtn" class="admin-btn">Reset to Original</button>
</div>

<script>
    // --- MASTER DATA ---
    let products = [
        { id: 1, name: "Monochrome Silk Blazer", price: 349, category: "Fashion", isBestSeller: true, imgIcon: "fas fa-tshirt", featured: true },
        { id: 2, name: "GMT Wrist Chronograph", price: 599, category: "Accessories", isBestSeller: true, imgIcon: "fas fa-clock", featured: true },
        { id: 3, name: "Aura Leather Tote", price: 279, category: "Lifestyle", isBestSeller: false, imgIcon: "fas fa-bag-shopping", featured: true },
        { id: 4, name: "Minimalist Sneakers", price: 189, category: "Trending", isBestSeller: true, imgIcon: "fas fa-shoe-prints", featured: true },
        { id: 5, name: "Gold Frame Sunglasses", price: 229, category: "Accessories", isBestSeller: false, imgIcon: "fas fa-glasses", featured: false },
        { id: 6, name: "Cashmere Scarf", price: 159, category: "Fashion", isBestSeller: true, imgIcon: "fas fa-scarf", featured: false },
        { id: 7, name: "Aesthetic Candle Set", price: 89, category: "Lifestyle", isBestSeller: false, imgIcon: "fas fa-fire", featured: false },
        { id: 8, name: "Tech Organizer", price: 99, category: "Trending", isBestSeller: false, imgIcon: "fas fa-laptop", featured: false },
    ];
    let cart = [];
    let wishlistIds = new Set();
    let testimonials = [
        { name: "Aarav K.", text: "Exquisite craftsmanship. The blazer feels bespoke.", rating: "★★★★★" },
        { name: "Ishita R.", text: "GM Store is my new luxury destination. Gold standard!", rating: "★★★★★" },
        { name: "Rohan M.", text: "Shipping was elegant and fast. Highly recommended.", rating: "★★★★☆" }
    ];
    let offerText = { title: "✦ LIMITED OFFER ✦", desc: "Get 15% off on first order | Code: GMGOLD15" };
    let aboutHTML = "<p>GM Store represents the pinnacle of modern luxury — curated by Gautam Maheshwari. Each piece blends timeless elegance with contemporary design.</p>";

    // Helper functions
    function showToast(msg) { let t = document.createElement('div'); t.className = 'toast-msg'; t.innerText = msg; document.body.appendChild(t); setTimeout(() => t.remove(), 2000); }
    function saveToLocal() { localStorage.setItem('gm_products', JSON.stringify(products)); localStorage.setItem('gm_testimonials', JSON.stringify(testimonials)); }
    function loadFromLocal() { let p = localStorage.getItem('gm_products'); if(p) products = JSON.parse(p); let t = localStorage.getItem('gm_testimonials'); if(t) testimonials = JSON.parse(t); }
    function renderAll() { renderFeatured(); renderBestSellers(); renderCategories(); renderTestimonials(); updateCartUI(); renderAdminProductList(); }
    function renderFeatured() { const featuredProds = products.filter(p => p.featured); document.getElementById('featuredGrid').innerHTML = featuredProds.map(p => productCardHTML(p)).join(''); attachEvents(); }
    function renderBestSellers() { const best = products.filter(p => p.isBestSeller); document.getElementById('bestsellersGrid').innerHTML = best.map(p => productCardHTML(p)).join(''); attachEvents(); }
    function renderCategories() { const cats = [...new Set(products.map(p=>p.category))]; document.getElementById('categoriesGrid').innerHTML = cats.map(cat => `<div class="category-card" data-category="${cat}"><i class="fas ${cat==='Fashion'?'fa-vest':cat==='Accessories'?'fa-gem':cat==='Lifestyle'?'fa-couch':'fa-chart-line'}"></i><h3>${cat}</h3><p>Shop collection</p></div>`).join(''); document.querySelectorAll('.category-card').forEach(c => c.addEventListener('click',()=>showToast(`Explore ${c.dataset.category} collection`))); }
    function renderTestimonials() { document.getElementById('testimonialsGrid').innerHTML = testimonials.map(t => `<div class="testimonial-card"><i class="fas fa-gem" style="color:var(--gold); margin-bottom:0.5rem; display:inline-block;"></i><p>“${t.text}”</p><h4>${t.name}</h4><div>${t.rating}</div></div>`).join(''); }
    function productCardHTML(p) { const isWished = wishlistIds.has(p.id); return `<div class="product-card" data-id="${p.id}"><div class="product-img"><i class="${p.imgIcon}"></i></div><div class="product-info"><div class="product-title">${p.name}</div><div class="product-price">$${p.price}</div><div class="card-actions"><button class="btn-add" data-id="${p.id}">Add to cart</button><button class="btn-wish" data-id="${p.id}"><i class="${isWished ? 'fas' : 'far'} fa-heart"></i> Wish</button></div></div></div>`; }
    function attachEvents() { document.querySelectorAll('.btn-add').forEach(btn => btn.onclick = (e) => { e.stopPropagation(); const id = parseInt(btn.dataset.id); const prod = products.find(p=>p.id===id); if(prod){ let existing = cart.find(i=>i.id===id); existing ? existing.quantity++ : cart.push({...prod, quantity:1}); updateCartUI(); showToast(`Added ${prod.name} ✨`); } }); document.querySelectorAll('.btn-wish').forEach(btn => btn.onclick = (e) => { e.stopPropagation(); const id = parseInt(btn.dataset.id); wishlistIds.has(id) ? wishlistIds.delete(id) : wishlistIds.add(id); renderAll(); showToast(wishlistIds.has(id) ? "❤️ Wishlisted" : "Removed from wishlist"); }); }
    function updateCartUI() { const count = cart.reduce((s,i)=>s+i.quantity,0); document.getElementById('cartCount').innerText = count; const cartDiv = document.getElementById('cartItemsList'); if(cartDiv){ if(cart.length===0) cartDiv.innerHTML = '<p>Your bag is empty, elegant.</p>'; else { cartDiv.innerHTML = cart.map(item => `<div class="cart-item"><span>${item.name} x${item.quantity}</span><span>$${item.price*item.quantity}</span><button class="remove-item" data-id="${item.id}" style="background:none; color:var(--gold); border:none; cursor:pointer;">🗑️</button></div>`).join(''); document.querySelectorAll('.remove-item').forEach(btn => btn.onclick = () => { cart = cart.filter(i => i.id !== parseInt(btn.dataset.id)); updateCartUI(); showToast("Item removed"); }); } } const total = cart.reduce((s,i)=>s+(i.price*i.quantity),0); document.getElementById('cartTotal').innerHTML = `<strong>Total: $${total}</strong>`; }
    function renderAdminProductList() { const listDiv = document.getElementById('adminProductList'); if(listDiv) listDiv.innerHTML = products.map(p => `<div style="display:flex; justify-content:space-between; margin:0.5rem 0;"><span>${p.name} - $${p.price}</span><div><button class="admin-btn edit-prod" data-id="${p.id}">Edit</button><button class="admin-btn del-prod" data-id="${p.id}" style="margin-left:5px;">Delete</button></div></div>`).join(''); document.querySelectorAll('.edit-prod').forEach(btn => btn.onclick = () => { let id = parseInt(btn.dataset.id); let prod = products.find(p=>p.id===id); let newName = prompt("New name:", prod.name); if(newName) prod.name = newName; let newPrice = prompt("New price:", prod.price); if(newPrice) prod.price = parseFloat(newPrice); renderAll(); saveToLocal(); }); document.querySelectorAll('.del-prod').forEach(btn => btn.onclick = () => { let id = parseInt(btn.dataset.id); products = products.filter(p=>p.id !== id); renderAll(); saveToLocal(); showToast("Product deleted"); }); }

    // Admin: add product
    document.getElementById('addProductBtn')?.addEventListener('click',()=>{ let name = document.getElementById('newProdName').value; let price = parseFloat(document.getElementById('newProdPrice').value); let cat = document.getElementById('newProdCat').value; if(name && price){ let newId = Date.now(); products.push({ id:newId, name, price, category:cat, isBestSeller:false, imgIcon:"fas fa-gem", featured:true }); renderAll(); saveToLocal(); showToast("Product added!"); document.getElementById('newProdName').value=''; document.getElementById('newProdPrice').value=''; } else showToast("Enter name & price"); });
    document.getElementById('resetDataBtn')?.addEventListener('click',()=>{ location.reload(); });
    document.getElementById('applyColorBtn')?.addEventListener('click',()=>{ let newColor = document.getElementById('themeColorPicker').value; document.documentElement.style.setProperty('--gold', newColor); showToast("Theme color updated!"); });
    document.getElementById('editOfferBtn')?.addEventListener('click',()=>{ let newTitle = prompt("Offer Title:", offerText.title); let newDesc = prompt("Offer Description:", offerText.desc); if(newTitle && newDesc){ offerText.title=newTitle; offerText.desc=newDesc; document.querySelector('.offer-banner h3').innerText=newTitle; document.querySelector('.offer-banner p').innerHTML=newDesc; showToast("Offer updated"); } });
    document.getElementById('editAboutBtn')?.addEventListener('click',()=>{ let newAbout = prompt("Edit About Us text:", aboutHTML.replace(/<p>|<\/p>/g, '')); if(newAbout){ aboutHTML = `<p>${newAbout}</p>`; document.getElementById('aboutTextBlock').innerHTML = aboutHTML; showToast("About updated"); } });
    document.getElementById('addTestimonialBtn')?.addEventListener('click',()=>{ let name = prompt("Customer name:"); let text = prompt("Testimonial text:"); if(name && text){ testimonials.push({name, text, rating:"★★★★★"}); renderTestimonials(); saveToLocal(); showToast("Testimonial added"); } });
    // Cart & Checkout UI
    document.getElementById('cartIcon')?.addEventListener('click',()=>document.getElementById('cartSidebar').classList.add('open'));
    document.getElementById('closeCartBtn')?.addEventListener('click',()=>document.getElementById('cartSidebar').classList.remove('open'));
    document.getElementById('checkoutBtnSidebar')?.addEventListener('click',()=>{ document.getElementById('cartSidebar').classList.remove('open'); document.getElementById('checkoutPage').classList.add('open'); });
    document.getElementById('closeCheckoutBtn')?.addEventListener('click',()=>document.getElementById('checkoutPage').classList.remove('open'));
    document.getElementById('payNowBtn')?.addEventListener('click',()=>{ alert("✅ Payment simulation complete. Thank you for your luxury purchase!"); cart=[]; updateCartUI(); document.getElementById('checkoutPage').classList.remove('open'); showToast("Order placed. Welcome to GM Store."); });
    document.getElementById('adminControlBtn')?.addEventListener('click',()=>document.getElementById('adminPanel').classList.add('open'));
    document.getElementById('closeAdminBtn')?.addEventListener('click',()=>document.getElementById('adminPanel').classList.remove('open'));
    // Search
    document.getElementById('searchInput')?.addEventListener('input',(e)=>{ let term = e.target.value.toLowerCase(); let filtered = products.filter(p=>p.name.toLowerCase().includes(term)); document.getElementById('featuredGrid').innerHTML = filtered.map(p=>productCardHTML(p)).join(''); attachEvents(); document.getElementById('bestsellersGrid').innerHTML = filtered.filter(p=>p.isBestSeller).map(p=>productCardHTML(p)).join(''); attachEvents(); });
    // smooth navigation
    document.querySelectorAll('[data-nav]').forEach(link=>{ link.addEventListener('click',()=>{ let id = link.getAttribute('data-nav'); if(id==='home') window.scrollTo({top:0,behavior:'smooth'}); else if(id==='featured') document.getElementById('featured').scrollIntoView({behavior:'smooth'}); else if(id==='about') document.getElementById('about').scrollIntoView({behavior:'smooth'}); else if(id==='contact') document.getElementById('contact')?.scrollIntoView({behavior:'smooth'}); }); });
    loadFromLocal(); renderAll(); 
    document.getElementById('offerBanner').querySelector('h3').innerText = offerText.title; document.getElementById('offerBanner').querySelector('p').innerHTML = offerText.desc;
    document.getElementById('aboutTextBlock').innerHTML = aboutHTML;
</script>
</body>
</html>